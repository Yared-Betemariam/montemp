export const publicRoutes = ["/", "/terms", "/privacy-policy"];

export const apiAuthPrefix = "/api/auth";

export const authRoutes = ["/auth/sign-in", "/error", "/auth/new-verification"];

export const defauthLoginRedirect = "/dashboard";
